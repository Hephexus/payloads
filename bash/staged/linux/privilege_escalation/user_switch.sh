#!/bin/bash
# user_switch.sh - Attempt to switch users using the su command.
echo "=== User Switch Simulation ==="
echo "Attempting to switch user via su..."
su -c 'id'
echo "============================="
